
# Example backend .env file
# Fill in your actual secrets and connection info in your real .env (never commit .env)

PORT=5000
MONGO_URI=mongodb+srv://<username>:<password>@<host>/<database>?retryWrites=true&w=majority&appName=<cluster>

JWT_SECRET=your_jwt_secret_here

PLAID_CLIENT_ID=your_plaid_client_id
PLAID_SECRET=your_plaid_secret
PLAID_ENV=sandbox   # or development / production
